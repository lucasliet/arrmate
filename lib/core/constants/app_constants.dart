/// Very small padding (4.0).
const double paddingXs = 4.0;

/// Small padding (8.0).
const double paddingSm = 8.0;

/// Medium padding (16.0).
const double paddingMd = 16.0;

/// Large padding (24.0).
const double paddingLg = 24.0;

/// Extra large padding (32.0).
const double paddingXl = 32.0;

/// Small border radius (8.0).
const double radiusSm = 8.0;

/// Medium border radius (12.0).
const double radiusMd = 12.0;

/// Large border radius (16.0).
const double radiusLg = 16.0;

/// Extra large border radius (24.0).
const double radiusXl = 24.0;

/// Standard aspect ratio for movie/show posters (2:3).
const double posterAspectRatio = 2 / 3;

/// Standard aspect ratio for fanart/background images (16:9).
const double fanartAspectRatio = 16 / 9;

/// Spacing between items in grid views.
const double gridSpacing = 12.0;

/// Number of columns in grid for compact layouts (e.g., phones).
const int gridColumnsCompact = 3;

/// Number of columns in grid for large layouts (e.g., tablets).
const int gridColumnsLarge = 4;

/// Small icon size (16.0).
const double iconSizeSm = 16.0;

/// Medium icon size (24.0).
const double iconSizeMd = 24.0;

/// Large icon size (32.0).
const double iconSizeLg = 32.0;
